---
description: Spark energy-cost usage — periods, by session, and totals
---
Below is my opencode Spark energy-cost usage, generated by a local script.
Present it to me verbatim as three clean markdown tables (Periods, By session,
By provider/model) followed by the TOTAL lines. Do not analyze, summarize, or
add commentary — just reformat for readability. Preserve every number exactly.

Periods (today / this week / this month / all time):
!`bash ~/.config/opencode/bin/spark-total.sh --periods 2>&1`

By session:
!`bash ~/.config/opencode/bin/spark-total.sh --by-session 2>&1`

By provider/model:
!`bash ~/.config/opencode/bin/spark-total.sh 2>&1`
